import { SearchForm } from "@/components/search/SearchForm";
import { Logo } from "@/components/ui/Logo";
import { Plane, Zap, SlidersHorizontal } from "lucide-react";
import Link from "next/link";

export default function HomePage() {
  return (
    <div className="flex flex-col">
      <section className="relative flex min-h-[calc(100vh-57px)] flex-col items-center justify-center px-4 py-16">
        <div
          className="pointer-events-none absolute inset-0"
          style={{
            background: 'radial-gradient(ellipse 80% 50% at 50% 0%, rgba(99,102,241,0.12) 0%, transparent 70%)',
          }}
        />

        <div className="relative z-10 w-full max-w-2xl">
          <div className="mb-10 text-center">
            <div className="mb-4 flex justify-center">
              <Logo size="lg" />
            </div>
            <h1 className="font-display text-3xl font-bold tracking-tight text-white sm:text-4xl md:text-5xl">
              Flight search for{" "}
              <span className="text-[#6366f1]">power travelers</span>
            </h1>
            <p className="mt-4 text-base text-[#8888aa] sm:text-lg max-w-xl mx-auto">
              Multi-airport search, advanced layover controls, and price calendars.
              Find the best flights the way you want.
            </p>
          </div>

          <SearchForm />

          <div className="mt-6 flex justify-center">
            <Link
              href="/calendar"
              className="text-sm text-[#8888aa] hover:text-[#a5b4fc] transition-colors flex items-center gap-1.5"
            >
              <span>Explore dates on the price calendar</span>
              <span>→</span>
            </Link>
          </div>
        </div>
      </section>

      <section className="border-t border-[#2a2a3a] bg-[#111118] px-4 py-16">
        <div className="mx-auto max-w-5xl">
          <h2 className="font-display text-center text-2xl font-bold text-white mb-10">
            What makes Routr different
          </h2>
          <div className="grid gap-6 sm:grid-cols-3">
            <FeatureCard
              icon={<Plane className="h-5 w-5" />}
              title="Multi-airport search"
              description="Search up to 6 origin and destination airports simultaneously. Merge all results and get the best deal across every combination."
            />
            <FeatureCard
              icon={<SlidersHorizontal className="h-5 w-5" />}
              title="Advanced layover control"
              description="Set min and max layover times, include or exclude specific airports, and control overnight connections with precision."
            />
            <FeatureCard
              icon={<Zap className="h-5 w-5" />}
              title="Price calendar"
              description="See the cheapest days to fly at a glance. Color-coded pricing across a full month helps you pick the perfect travel window."
            />
          </div>
        </div>
      </section>
    </div>
  );
}

function FeatureCard({ icon, title, description }: { icon: React.ReactNode; title: string; description: string }) {
  return (
    <div className="rounded-xl border border-[#2a2a3a] bg-[#0a0a0f] p-5">
      <div className="mb-3 inline-flex rounded-lg bg-[#6366f1]/15 p-2.5 text-[#a5b4fc]">
        {icon}
      </div>
      <h3 className="font-display font-semibold text-white mb-2">{title}</h3>
      <p className="text-sm text-[#8888aa] leading-relaxed">{description}</p>
    </div>
  );
}
